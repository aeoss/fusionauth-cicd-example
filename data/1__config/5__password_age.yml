maximumPasswordAge: &maximum_password_age

  #
  # Indicates that the maximum password age is enabled and being enforced.
  #
  enabled: false

  #
  # The password maximum age in days. The number of days after which FusionAuth will require
  # a user to change their password.
  #
  days: null

minimumPasswordAge: &minimum_password_age

  #
  # Indicates that the maximum password age is enabled and being enforced.
  #
  enabled: false

  #
  # The password maximum age in days. The number of days after which FusionAuth will require
  # a user to change their password.
  #
  seconds: null
